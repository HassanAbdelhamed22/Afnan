import "server-only";
import { InvalidStateError, NotFoundError } from "@/lib/errors/app-error"; import { connectMongoose } from "@/lib/mongoose"; import { canTransitionCustomRequest } from "./admin-domain"; import { CustomRequestModel } from "./model"; import type { CustomRequestStatus } from "./dto";
export async function updateAdminCustomRequest(requestNumber: string, status: CustomRequestStatus, internalNotes?: string) { await connectMongoose(); const request = await CustomRequestModel.findOne({ requestNumber }); if (!request) throw new NotFoundError("Custom request not found"); if (!canTransitionCustomRequest(request.status, status)) throw new InvalidStateError(`Request cannot move from ${request.status} to ${status}`); request.status = status; request.internalNotes = internalNotes; await request.save(); return request.requestNumber; }
